// 19. Even numbers are divisible by 2 and the remainder is zero. How do you check, if a number 
// is even or not using JavaScript?

let x = 4

if (x % 2 == 0) {
    console.log(`${x} is even number`)
} else {
    console.log(`${x} is odd number`)
}